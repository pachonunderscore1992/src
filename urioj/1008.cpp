#include <bits/stdc++.h>

using namespace std;

int main(){
	int n,h;
	double gph;
	cin >> n >> h >> gph;
	cout << "NUMBER = " << n << endl;
	cout.precision(2);
	cout << fixed << "SALARY = U$ "<< gph*h << endl;
	return 0;
}